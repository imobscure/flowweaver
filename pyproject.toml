[project]
name = "flowweaver"
version = "0.1.0"
description = "Lightweight DAG-based workflow orchestrator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.setuptools.package-data]
flowweaver = ["py.typed"]
